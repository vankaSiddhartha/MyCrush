import React from 'react';
import NavBar from '../components/NavBar';

// Use function syntax to define a React component
const Home = () => {
    return (
        <NavBar />
    );
}

export default Home;
